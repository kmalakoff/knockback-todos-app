class window.PrioritiesCollection extends Backbone.Collection
	localStorage: new Store("priorities-knockback-extended") 		# Save all of the settings under the `"priorities-knockback-extended"` namespace.